# DNS_Server_Simulation

This is a Practical Implementation of DNS Server working 
It is an Termial based project where user can enter the website name he wish to view like we do in google seach tab and then get the url link by searching in the JSON file, after searching the requested site the program returns therequested site in the form of clickable url link featched from the JSON, which after clicking redirects directly to the requested site 
